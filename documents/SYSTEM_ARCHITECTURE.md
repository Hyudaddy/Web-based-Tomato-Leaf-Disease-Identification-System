# Fito Admin Dashboard - System Architecture

## 🏗️ Architecture Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER INTERFACE                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │   /fito      │  │ /admin/login │  │   /admin     │         │
│  │ Upload Image │  │ Admin Login  │  │  Dashboard   │         │
│  └──────────────┘  └──────────────┘  └──────────────┘         │
│                                                                 │
│                    ┌──────────────────┐                        │
│                    │ /admin/dataset   │                        │
│                    │ Dataset Manager  │                        │
│                    └──────────────────┘                        │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    FRONTEND (Next.js 16)                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Components:                    Libraries:                      │
│  • AdminLayout                  • React 19                      │
│  • Navigation                   • TypeScript                    │
│  • BackgroundProvider           • Tailwind CSS                  │
│  • LoadingScreen                • Lucide Icons                  │
│                                                                 │
│  Supabase Client:                                               │
│  • Authentication                                               │
│  • Database queries                                             │
│  • Storage access                                               │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    BACKEND (FastAPI)                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Public Endpoints:              Admin Endpoints:                │
│  • POST /predict                • GET /api/admin/stats          │
│  • GET /health                  • GET /api/admin/dataset        │
│  • GET /classes                 • PATCH /api/admin/dataset/:id  │
│                                 • DELETE /api/admin/dataset/:id │
│                                 • GET /api/admin/dataset/export │
│                                                                 │
│  Model Handler:                 Supabase Client:                │
│  • TensorFlow/Keras             • Database operations           │
│  • Image preprocessing          • Storage operations            │
│  • Prediction logic             • Service role key              │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    SUPABASE (Backend as a Service)              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────────┐         ┌──────────────────┐            │
│  │   PostgreSQL     │         │   Storage        │            │
│  │   Database       │         │   (S3-like)      │            │
│  ├──────────────────┤         ├──────────────────┤            │
│  │                  │         │                  │            │
│  │ predictions      │         │ tomato-leaves/   │            │
│  │ ├─ id            │         │ ├─ Healthy/      │            │
│  │ ├─ storage_path  │         │ ├─ Bacterial/    │            │
│  │ ├─ image_url     │         │ ├─ Early Blight/ │            │
│  │ ├─ predicted_    │         │ ├─ Late Blight/  │            │
│  │ │  label         │         │ └─ ...           │            │
│  │ ├─ confidence    │         │                  │            │
│  │ ├─ final_label   │         │ Public URLs      │            │
│  │ ├─ uploader_name │         │ CDN delivery     │            │
│  │ ├─ created_at    │         │                  │            │
│  │ └─ updated_at    │         │                  │            │
│  │                  │         │                  │            │
│  └──────────────────┘         └──────────────────┘            │
│                                                                 │
│  ┌──────────────────────────────────────────────┐              │
│  │   Authentication (Auth)                      │              │
│  ├──────────────────────────────────────────────┤              │
│  │                                              │              │
│  │ • Email/Password authentication              │              │
│  │ • JWT token management                       │              │
│  │ • User metadata (role: admin)                │              │
│  │ • Session management                         │              │
│  │                                              │              │
│  └──────────────────────────────────────────────┘              │
│                                                                 │
│  ┌──────────────────────────────────────────────┐              │
│  │   Row Level Security (RLS)                   │              │
│  ├──────────────────────────────────────────────┤              │
│  │                                              │              │
│  │ • Public read on predictions                 │              │
│  │ • Public insert on predictions               │              │
│  │ • Authenticated update/delete                │              │
│  │ • Storage bucket policies                    │              │
│  │                                              │              │
│  └──────────────────────────────────────────────┘              │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow Diagrams

### 1. Image Upload & Prediction Flow

```
┌─────────┐
│  User   │
└────┬────┘
     │ 1. Uploads image
     ▼
┌─────────────────┐
│ /fito page      │
│ (Next.js)       │
└────┬────────────┘
     │ 2. POST /predict
     ▼
┌─────────────────────────┐
│ FastAPI Backend         │
│ ┌─────────────────────┐ │
│ │ Model Handler       │ │
│ │ • Preprocess image  │ │
│ │ • Run inference     │ │
│ │ • Get prediction    │ │
│ └─────────────────────┘ │
└────┬────────────────────┘
     │ 3. Save to Supabase
     ▼
┌─────────────────────────┐
│ Supabase                │
│ ┌─────────────────────┐ │
│ │ Storage             │ │
│ │ • Upload image      │ │
│ │ • Get public URL    │ │
│ └─────────────────────┘ │
│ ┌─────────────────────┐ │
│ │ Database            │ │
│ │ • Insert record     │ │
│ │ • Store metadata    │ │
│ └─────────────────────┘ │
└────┬────────────────────┘
     │ 4. Return prediction
     ▼
┌─────────────────┐
│ User sees:      │
│ • Prediction    │
│ • Confidence    │
│ • Details       │
└─────────────────┘
```

### 2. Admin Dashboard Flow

```
┌─────────┐
│  Admin  │
└────┬────┘
     │ 1. Navigate to /admin
     ▼
┌─────────────────────────┐
│ AdminLayout             │
│ • Check authentication  │
│ • Verify admin role     │
└────┬────────────────────┘
     │ 2. Authenticated
     ▼
┌─────────────────────────┐
│ Dashboard Page          │
│ • Fetch stats           │
└────┬────────────────────┘
     │ 3. GET /api/admin/stats
     ▼
┌─────────────────────────┐
│ FastAPI Backend         │
│ • Query predictions     │
│ • Count by category     │
└────┬────────────────────┘
     │ 4. Query database
     ▼
┌─────────────────────────┐
│ Supabase Database       │
│ • SELECT all records    │
│ • Group by label        │
└────┬────────────────────┘
     │ 5. Return stats
     ▼
┌─────────────────────────┐
│ Dashboard displays:     │
│ • Total: 150 images     │
│ • Healthy: 45           │
│ • Bacterial Spot: 23    │
│ • Early Blight: 18      │
│ • ... (7 more)          │
└─────────────────────────┘
```

### 3. Dataset Management Flow

```
┌─────────┐
│  Admin  │
└────┬────┘
     │ 1. Navigate to /admin/dataset
     ▼
┌─────────────────────────┐
│ Dataset Page            │
│ • Apply filters         │
│ • Set pagination        │
└────┬────────────────────┘
     │ 2. GET /api/admin/dataset?category=Healthy&page=1
     ▼
┌─────────────────────────┐
│ FastAPI Backend         │
│ • Build query           │
│ • Apply filters         │
│ • Paginate results      │
└────┬────────────────────┘
     │ 3. Query with filters
     ▼
┌─────────────────────────┐
│ Supabase Database       │
│ • WHERE category        │
│ • ORDER BY created_at   │
│ • LIMIT 20 OFFSET 0     │
└────┬────────────────────┘
     │ 4. Return paginated data
     ▼
┌─────────────────────────┐
│ Table displays:         │
│ ┌───────────────────┐   │
│ │ Thumbnail | Label │   │
│ │ [img]     | H...  │   │
│ │ [img]     | H...  │   │
│ │ ...       | ...   │   │
│ └───────────────────┘   │
│ [Prev] Page 1/8 [Next]  │
└─────────────────────────┘
```

### 4. Relabel Action Flow

```
┌─────────┐
│  Admin  │
└────┬────┘
     │ 1. Click relabel button
     ▼
┌─────────────────────────┐
│ Dataset Page            │
│ • Show dropdown         │
│ • Select new label      │
└────┬────────────────────┘
     │ 2. PATCH /api/admin/dataset/{id}/label
     │    body: { label: "Early Blight" }
     ▼
┌─────────────────────────┐
│ FastAPI Backend         │
│ • Validate label        │
│ • Update record         │
└────┬────────────────────┘
     │ 3. UPDATE predictions
     ▼
┌─────────────────────────┐
│ Supabase Database       │
│ UPDATE predictions      │
│ SET final_label = '...' │
│ WHERE id = '...'        │
└────┬────────────────────┘
     │ 4. Return updated record
     ▼
┌─────────────────────────┐
│ Table updates:          │
│ • Final label changed   │
│ • Timestamp updated     │
│ • Success message       │
└─────────────────────────┘
```

---

## 📁 File Structure

```
TLDI_system/
│
├── frontend/                       # Next.js Frontend
│   ├── src/
│   │   ├── app/
│   │   │   ├── layout.tsx         # Root layout
│   │   │   ├── page.tsx           # Home page
│   │   │   ├── fito/
│   │   │   │   └── page.tsx       # Upload & predict
│   │   │   └── admin/
│   │   │       ├── page.tsx       # Dashboard ⭐
│   │   │       ├── login/
│   │   │       │   └── page.tsx   # Admin login ⭐
│   │   │       └── dataset/
│   │   │           └── page.tsx   # Dataset manager ⭐
│   │   ├── components/
│   │   │   ├── Navigation.tsx
│   │   │   ├── AdminLayout.tsx    # Admin sidebar ⭐
│   │   │   ├── BackgroundProvider.tsx
│   │   │   └── LoadingScreen.tsx
│   │   └── lib/
│   │       └── supabase.ts        # Supabase client ⭐
│   ├── .env.local                 # Environment vars ⭐
│   └── package.json
│
├── backend/                        # FastAPI Backend
│   ├── app.py                     # Main app (modified) ⭐
│   ├── admin_routes.py            # Admin endpoints ⭐
│   ├── supabase_client.py         # Supabase helper ⭐
│   ├── model_handler.py           # ML model
│   ├── trained_model_fito.h5      # Model weights
│   ├── .env                       # Environment vars ⭐
│   └── requirements.txt           # Dependencies (updated) ⭐
│
├── docs/                           # Documentation
│   ├── QUICK_START.md             # 5-min setup guide ⭐
│   ├── SUPABASE_SETUP.md          # Detailed setup ⭐
│   ├── ADMIN_DASHBOARD_README.md  # Feature docs ⭐
│   ├── ADMIN_IMPLEMENTATION_SUMMARY.md ⭐
│   └── SYSTEM_ARCHITECTURE.md     # This file ⭐
│
└── assets/                         # Images and logos

⭐ = New or modified for admin dashboard
```

---

## 🔐 Security Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                        SECURITY LAYERS                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Layer 1: Frontend Authentication                               │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ • Supabase Auth check on admin routes                   │   │
│  │ • Redirect to login if not authenticated                │   │
│  │ • Check user metadata for admin role                    │   │
│  │ • Session persistence with JWT                          │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  Layer 2: API Security                                          │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ • CORS configuration (allow specific origins)           │   │
│  │ • Input validation on all endpoints                     │   │
│  │ • Error handling (no sensitive data in errors)          │   │
│  │ • Rate limiting (recommended for production)            │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  Layer 3: Database Security (RLS)                               │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ • Row Level Security enabled                            │   │
│  │ • Public read access (for app functionality)            │   │
│  │ • Public insert (for predictions)                       │   │
│  │ • Authenticated-only update/delete                      │   │
│  │ • Service role key only in backend                      │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  Layer 4: Storage Security                                      │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ • Public bucket (for image serving)                     │   │
│  │ • Upload policy (authenticated or service role)         │   │
│  │ • Delete policy (authenticated only)                    │   │
│  │ • File type validation                                  │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🚀 Deployment Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                      PRODUCTION SETUP                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Frontend (Vercel / Netlify)                                    │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ • Next.js build                                         │   │
│  │ • Edge functions                                        │   │
│  │ • CDN distribution                                      │   │
│  │ • Environment variables                                 │   │
│  │ • Custom domain                                         │   │
│  └─────────────────────────────────────────────────────────┘   │
│                          │                                      │
│                          ▼                                      │
│  Backend (Railway / Render / AWS)                               │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ • Docker container                                      │   │
│  │ • Gunicorn + Uvicorn workers                            │   │
│  │ • Load balancer                                         │   │
│  │ • Environment variables                                 │   │
│  │ • Health checks                                         │   │
│  └─────────────────────────────────────────────────────────┘   │
│                          │                                      │
│                          ▼                                      │
│  Supabase (Managed)                                             │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ • PostgreSQL database                                   │   │
│  │ • S3-compatible storage                                 │   │
│  │ • Authentication service                                │   │
│  │ • Automated backups                                     │   │
│  │ • CDN for images                                        │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📊 Database Schema Diagram

```
┌──────────────────────────────────────────────────────────┐
│                    predictions                           │
├──────────────────────────────────────────────────────────┤
│ id                UUID PRIMARY KEY                       │
│ storage_path      TEXT NOT NULL                          │
│ image_url         TEXT                                   │
│ predicted_label   TEXT NOT NULL ────┐                    │
│ confidence        FLOAT NOT NULL    │                    │
│ final_label       TEXT              │ (can be different) │
│ uploader_id       UUID ─────────────┼──────────┐         │
│ uploader_name     TEXT              │          │         │
│ created_at        TIMESTAMPTZ       │          │         │
│ updated_at        TIMESTAMPTZ       │          │         │
└───────────────────────────────────────────────┼──────────┘
                                                │
                                                ▼
                                    ┌────────────────────┐
                                    │   auth.users       │
                                    ├────────────────────┤
                                    │ id    UUID         │
                                    │ email TEXT         │
                                    │ metadata JSONB     │
                                    │   └─ role: admin   │
                                    └────────────────────┘

Indexes:
• idx_predictions_predicted_label  (predicted_label)
• idx_predictions_final_label      (final_label)
• idx_predictions_created_at       (created_at DESC)
```

---

## 🎯 Component Hierarchy

```
App (layout.tsx)
│
├── Navigation
│   └── Links to all pages
│
├── BackgroundProvider
│   └── Dynamic backgrounds
│
├── Public Pages
│   ├── Home (/)
│   ├── About (/about)
│   ├── FAQ (/faq)
│   ├── Contact (/contact)
│   ├── Information (/information)
│   └── Fito (/fito)
│       └── Upload & Predict
│
└── Admin Pages (/admin/*)
    │
    ├── Login (/admin/login)
    │   └── Email/Password form
    │
    └── AdminLayout (protected)
        │
        ├── Sidebar
        │   ├── Dashboard link
        │   ├── Dataset link
        │   └── Logout button
        │
        ├── Dashboard (/admin)
        │   ├── Total counter
        │   └── Category cards (10)
        │
        └── Dataset (/admin/dataset)
            ├── Filters
            │   ├── Category dropdown
            │   └── Search input
            ├── Table
            │   ├── Headers
            │   └── Rows (20 per page)
            │       ├── Thumbnail
            │       ├── Labels
            │       ├── Confidence
            │       ├── Date
            │       └── Actions
            │           ├── Preview modal
            │           ├── Relabel dropdown
            │           ├── Download button
            │           └── Delete button
            ├── Pagination
            │   ├── Previous button
            │   ├── Page indicator
            │   └── Next button
            └── Export buttons
                ├── CSV export
                └── ZIP export
```

---

## 🔄 State Management

```
Frontend State (React Hooks):

Dashboard Page:
├── stats: CategoryStats[]
├── loading: boolean
└── totalImages: number

Dataset Page:
├── predictions: Prediction[]
├── filteredPredictions: Prediction[]
├── loading: boolean
├── selectedCategory: string
├── searchQuery: string
├── selectedImage: Prediction | null
├── relabelId: string | null
├── newLabel: string
├── currentPage: number
└── itemsPerPage: number (constant: 20)

AdminLayout:
├── isAdmin: boolean
├── loading: boolean
└── user: User | null

No global state management needed (simple app)
All state is local to components
Supabase handles auth state
```

---

## 📈 Performance Considerations

```
Frontend Optimizations:
• Next.js Image component for optimized images
• Pagination (20 items) to limit DOM size
• Lazy loading for images
• Debounced search input
• Memoized filter functions

Backend Optimizations:
• Database indexes on frequently queried columns
• Pagination with LIMIT/OFFSET
• Efficient Supabase queries
• Image streaming for downloads
• Async/await for non-blocking I/O

Database Optimizations:
• Indexes on predicted_label, final_label, created_at
• RLS policies for security without performance hit
• Connection pooling (Supabase handles this)

Storage Optimizations:
• CDN delivery via Supabase
• Public URLs for fast access
• Organized folder structure by category
```

---

## 🎨 Design System

```
Colors:
• Primary: Green (#10B981, #059669)
• Secondary: Gray (#6B7280, #374151)
• Success: Green (#10B981)
• Error: Red (#EF4444)
• Warning: Yellow (#F59E0B)
• Info: Blue (#3B82F6)

Typography:
• Font: Inter (body), Montserrat (headings)
• Sizes: text-sm, text-base, text-lg, text-xl, text-2xl, text-3xl

Spacing:
• Padding: p-2, p-4, p-6, p-8
• Margin: m-2, m-4, m-6, m-8
• Gap: gap-2, gap-4, gap-6

Components:
• Buttons: rounded-lg, px-4, py-2
• Cards: rounded-lg, shadow-sm, border
• Inputs: rounded-lg, border, focus:ring-2
• Tables: border-collapse, hover:bg-gray-50
```

---

This architecture provides a solid foundation for the Fito admin dashboard with room for future enhancements!

